## Change

In `src/pages/Login.tsx`, restyle the submit button:

- Replace `rounded-xl` with `rounded-full` for a fully rounded pill shape.
- Change label from "Login" / "Logging in..." to "Get Started" / "Getting started...".
- Preserve current amber gradient, text color, shadow, height, and hover states — no other visual identity changes.

No other files, routes, or logic touched.